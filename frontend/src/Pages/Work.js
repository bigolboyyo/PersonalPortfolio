import React from "react";
import { Typography } from "@mui/material";

const Work = () => {
  return (
    <div className="page">
      <Typography variant="h1">Work</Typography>
    </div>
  );
};

export default Work;
