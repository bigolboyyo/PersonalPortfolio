import React from "react";
import { Typography } from "@mui/material";

const Home = () => {
  return (
    <div className="page">
      <Typography variant="h1">Home</Typography>
    </div>
  );
};

export default Home;
